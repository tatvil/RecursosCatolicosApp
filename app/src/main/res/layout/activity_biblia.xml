<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/deep_night"
    tools:context=".BibliaActivity">

    <!-- Header / Title -->
    <TextView
        android:id="@+id/textTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/santa_biblia"
        android:textColor="@color/soft_gold"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/selectorsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:weightSum="2"
        app:layout_constraintTop_toBottomOf="@id/textTitle">

        <Spinner
            android:id="@+id/spinner_books"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1.2"
            android:layout_marginEnd="8dp"
            android:backgroundTint="@color/soft_gold"
            android:overlapAnchor="false" />

        <Spinner
            android:id="@+id/spinner_chapters"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="0.8"
            android:backgroundTint="@color/soft_gold"
            android:overlapAnchor="false" />
    </LinearLayout>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        app:cardBackgroundColor="@color/dark_charcoal"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/selectorsLayout"
        app:layout_constraintBottom_toTopOf="@+id/buttonSiguienteVersiculo">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            android:fadeScrollbars="true">

            <TextView
                android:id="@+id/text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/versiculo_de_la_biblia"
                android:textSize="20sp"
                android:lineSpacingExtra="8dp"
                android:textColor="@color/prayer_cream"
                android:gravity="center"
                android:fontFamily="serif" />
        </ScrollView>
    </androidx.cardview.widget.CardView>

    <Button
        android:id="@+id/buttonSiguienteVersiculo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/siguiente_versiculo"
        android:backgroundTint="@color/soft_gold"
        android:textColor="@color/deep_night"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>